import { describe, it, expect } from 'vitest';import { ValidatorNotRegisteredError } from '../validator-not-registered.error';describe('ValidatorNotRegisteredError', () => {  it('should create an instance for 'uuid'', () => {    const error = new ValidatorNotRegisteredError('uuid');    expect(error).toBeInstanceOf(ValidatorNotRegisteredError);    expect(error.name).toBe('ValidatorNotRegisteredError');    expect(error.type).toBe('uuid');    expect(error.message).toBe('Not validator registered for type "uuid"');  });  it('should create an instance for 'rfc'', () => {    const error = new ValidatorNotRegisteredError('rfc');    expect(error).toBeInstanceOf(ValidatorNotRegisteredError);    expect(error.name).toBe('ValidatorNotRegisteredError');    expect(error.type).toBe('rfc');    expect(error.message).toBe('Not validator registered for type "rfc"');  });});